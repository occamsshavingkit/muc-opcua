# Pre-Implementation Brief Report: Nano Client - Discovery + Read

**Date**: 2026-07-07
**Feature**: [`specs/047-nano-client/spec.md`](../spec.md)
**Roadmap**: [`.specify/memory/roadmap.md`](../../../.specify/memory/roadmap.md) entry `001 - Nano Client: Discovery + Read`
**Verdict**: ⚠️ PROCEED WITH UPDATES

---

## Summary

Reviewed the active feature `specs/047-nano-client` against the client-side roadmap. The active branch/spec maps to roadmap entry `001 - Nano Client: Discovery + Read`, not entry `002 - Micro Client: Subscriptions`. The Nano spec is substantially aligned with the roadmap intent and is already at implementation readiness with spec, plan, research, data model, contracts, quickstart, checklist, and tasks present. The roadmap ledger is stale because it still marks entry 001 as `planned` and says the spec directory is not yet created.

---

## Surfaced Context

- **Entry**: `001 - Nano Client: Discovery + Read` · **Status**: `planned`
- **Description**: Minimal viable OPC UA client implementing the Nano Embedded UA Client Profile: endpoint discovery, SecureChannel open/close, session create/activate/close, variable reads, and address-space browsing.
- **Outcome (expected)**: A C client can connect to any standard OPC UA server, authenticate, read values, and navigate the node tree via Browse.
- **Scope (in)**: FindServers/GetEndpoints; OpenSecureChannel/CloseSecureChannel; CreateSession/ActivateSession/CloseSession; Read; Browse/BrowseNext; OPC UA Binary on `opc.tcp`; SecurityPolicy None and Basic256Sha256; host tests and interop against the existing server.
- **Scope (out)**: Subscriptions/MonitoredItems; Write; Call; HistoryRead; Events/Alarms; PubSub; JSON; HTTPS.
- **Depends on**: none.
- **Governed by**: `C-01` embedded-first C core; `C-02` shared infrastructure; `C-03` profile gating via CMake; `C-04` client/server state isolation. ADR links `ADR-0001`, `ADR-0002`, and `ADR-0003` are unresolved because `docs/adr/` is not present.
- **Addresses**: no PRD pointer recorded.
- **Related open questions / cross-cutting notes**: Q2 client-side SecurityPolicy negotiation and Q3 shared transport model remain relevant. Cross-cutting notes require shared binary encoder/decoder, StatusCode system, NodeId/Variant types, and security policy stubs; no forked shared code.

## Findings

| ID | Severity | Category | Finding | Suggestion |
|----|----------|----------|---------|------------|
| F1 | 💡 Recommendation | roadmap-stale | Roadmap entry 001 still says `status: planned` and `Spec dir: _not yet created_`, but `specs/047-nano-client/` exists with complete planning artifacts and tasks. | Update entry 001 via `/speckit.roadmap.write` to point at `specs/047-nano-client/` and move status to `in-progress` before implementation. |
| F2 | 🤔 Question | dependency-sequencing | The user referred to the "micro client", but roadmap entry 002 is Micro Client: Subscriptions and depends on entry 001 Nano Client. The active feature is Nano, not Micro. | Confirm whether to continue implementing Nano first, or create a new Micro subscriptions spec after Nano reaches implemented/verified. |
| F3 | 💡 Recommendation | unresolved-governance | Roadmap references `ADR-0001`, `ADR-0002`, and `ADR-0003`, but `docs/adr/` is absent, so the ADR constraints cannot be resolved from disk. | Either add the ADR files or amend the roadmap to rely only on `C-01` through `C-04` until ADRs exist. |

## Findings Summary

| Severity | Count |
|----------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendation | 2 |
| 🤔 Question | 1 |

## Recommended Actions

1. **F1**: Apply the roadmap status/spec-dir correction via `/speckit.roadmap.write` before implementation.
2. **F2**: Treat `specs/047-nano-client` as the current active workflow unless the user explicitly chooses to skip ahead to roadmap entry 002.
3. **F3**: Resolve or remove the ADR references before using them as implementation gates.
4. Proceed with the next Spec Kit gate for the active Nano feature: run `/speckit.analyze` against `specs/047-nano-client/tasks.md`, then fix all findings above LOW before implementation.

---

**Severity Legend**: 🎯 Must-Address (blocks proceeding) · 💡 Recommendation (strongly suggested) · 🤔 Question (needs a decision). **This report is read-only**; apply any roadmap changes via `/speckit.roadmap.write`.

*Generated by `/speckit.roadmap.brief`.*
