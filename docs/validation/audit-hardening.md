# Audit Hardening Validation

Feature: `020-audit-hardening`

This file is the validation evidence ledger for the active audit-hardening
feature. It is a checklist skeleton: `TBD` values are intentional placeholders
for later validation tasks and do not claim that any command has passed.

Sources:

- `specs/020-audit-hardening/spec.md`
- `specs/020-audit-hardening/plan.md`
- `specs/020-audit-hardening/quickstart.md`
- `docs/traceability/020-audit-hardening.md`
- `docs/size/audit-hardening-baseline.md`

## Current Blocker-Remediation Refresh

Updated 2026-06-30 after the open blocker rows were fixed. Older T085-T100
transcripts remain in this file as historical pre-remediation evidence when
called out that way; the active blocker status is the refresh below.

- Host and sanitizer tests: full host CTest and full ASan/UBSan CTest both pass
  `95/95` targets. Focused sanitizer HistoryRead and stale CreateSession
  integration reruns also pass.
- Fuzz coverage: `tests/fuzz/fuzz_binary_types.c` is no longer a placeholder or
  input-discard target; the refreshed libFuzzer smoke for
  `build-fuzz/tests/fuzz/fuzz_binary_types -runs=2` passes with non-startup
  coverage (`cov: 11`, `ft: 12`).
- Embedded/resource evidence: optional-service matrix, ARM nano/micro/embedded/
  full-featured size matrix, Pico embedded build, Pico stack check, host stack
  check, host/full optimized-size archive, static-RAM symbol review, and
  source-level core hot-path heap review all have current passing or recorded
  evidence.
- Source-ID closure: `docs/validation/audit-hardening-triage-ledger.md` now
  records repository-owned source finding IDs and severities for every closure
  row; `docs/validation/audit-hardening-closure.md` records no active
  repository-fixable blocker rows after T102c.

## Host-Test Validation Checklist

Coverage intent: this host-test gate primarily supports FR-020 and SC-006. The
focused host rows also provide later evidence hooks for malformed-input,
state-mutation, security, service-semantics, and closure coverage under
SC-001 through SC-004.

T005a did not run these commands. T085b recorded the original host command
transcript on 2026-06-30. The focused host gate passed then, and the T102b
refresh cleared the conformance-doc blocker by replacing the
`fuzz_binary_types` placeholder harness. Formatting/static-analysis and latency
have current evidence in their own sections.

### Required Host Commands

| Check | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- |
| Host build | `cmake --build build/test` | Build dir; generator/compiler; commit; exit code; warnings-as-errors outcome; produced host test executables. | PASS | T093a host build evidence: exit code `0` recorded in `/tmp/muc-opcua-t093a/host-build.exit`; transcript `/tmp/muc-opcua-t093a/host-build.log` shows targets built through `audit_latency_benchmark`; quick artifact check found 95 executable files under `build/test/tests`; no warning/error diagnostics were found in the saved transcript. T102b full host CTest evidence also passed 95/95. |
| Focused US1-US3 host CTest group | `ctest --test-dir build/test -R '^(test_discovery_services|test_discovery_endpoint|test_dispatch_services|test_session|test_security_identity_errors|test_read_service|test_write_errors|test_write_decoder|test_write_service_integration|test_browse_service|test_browse_limits|test_subscriptions_errors|test_subscriptions|test_query_service|test_query_encoding|test_history|test_secure_channel|test_secure_handshake_modern|test_connection_multiplex|test_single_client_limit|test_user_auth_plaintext|test_aggregate|test_node_management_errors|test_node_management|test_service_dispatch)$' --output-on-failure` | CTest exit code; total/passed/failed/skipped test count; failing test names; duration; transcript path; `build/test/Testing/Temporary/LastTest.log` path. | PASS | T094a host evidence: command transcript `/tmp/muc-opcua-t094a/host-ctest.log`; exit code `0`; 25/25 CTest targets passed, 0 failed, skipped not reported, no failing tests, total real time `0.40 sec`; `LastTest.log` path `build/test/Testing/Temporary/LastTest.log`. T102b full host CTest evidence passed 95/95 after the integration fixture refresh. |
| Write-service integration rerun | `ctest --test-dir build/test -R '^test_write_service_integration$' --output-on-failure` | CTest exit code; total/passed/failed/skipped test count; failing test names; duration; transcript path; stale-token fixture regression status. | PASS | T085b host evidence: exit code `0`; 1/1 CTest target passed, 0 failed, total test time `0.00 sec`; latest CTest transcript path `build/test/Testing/Temporary/LastTest.log` is overwritten by subsequent CTest runs. The earlier write integration failure is no longer current after stale hardcoded authentication-token fixtures were fixed. |
| Conformance documentation subgroup | `ctest --test-dir build/test -R '^test_(conformance_docs|traceability_docs)$' --output-on-failure` | CTest exit code; total/passed/failed/skipped count; failing test names; failure reason; duration; `build/test/Testing/Temporary/LastTest.log` path. | PASS | T085b originally failed only on the former `fuzz_binary_types` placeholder/input-discard blocker. T102b refresh replaced that harness, then `cmake --build build/test --target test_conformance_docs` and `ctest --test-dir build/test -R '^test_conformance_docs$' --output-on-failure` passed. Use the combined subgroup command above for fresh full doc-gate transcripts. |

### Focused Host Evidence Rows

Use these focused commands after the referenced tests exist. If a later task
renames a test target, update the relevant row before recording evidence.

| Evidence row | Coverage | Command | Expected result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| US1 Discovery negative paths | FR-001, FR-002, FR-011, SC-001, SC-002, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(discovery_services|discovery_endpoint|dispatch_services)'` | Exact StatusCodes; no state mutation; test count; failures. | PASS | T042a runner evidence: `cmake --build build/test --target test_discovery_services` passed; `ctest --test-dir build/test -R '^test_discovery_services$' --output-on-failure` passed with `test_discovery_services`, 1/1 test passed, 0 failed, total test time 0.00 sec. |
| US1 Session and identity decoding | FR-002, FR-003, FR-008, SC-001, SC-002, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(session|security_identity_errors|dispatch_services)'` | `CreateSession` no-allocation failures; bounded identity-token decode; test count; failures. | PASS | T042c runner evidence: `cmake --build build/test --target test_session test_security_identity_errors test_dispatch_services` passed with exit code 0; `ctest --test-dir build/test -R '^(test_session|test_security_identity_errors|test_dispatch_services)$' --output-on-failure` passed with exit code 0, 3/3 tests passed, 0 failed, total test time 0.00 sec. Unity counts from `build/test/Testing/Temporary/LastTest.log`: `test_dispatch_services` 14 Tests, 0 Failures, 0 Ignored; `test_security_identity_errors` 3 Tests, 0 Failures, 0 Ignored; `test_session` 4 Tests, 0 Failures, 0 Ignored. |
| US1 Read validation | FR-002, FR-012, SC-001, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_read_service'` | Invalid `TimestampsToReturn` StatusCode; test count; failures. | PASS | T042e runner evidence: `cmake --build build/test --target test_read_service` passed with exit code 0; `ctest --test-dir build/test -R '^test_read_service$' --output-on-failure` passed with exit code 0, 1/1 test passed, 0 failed, total test time 0.00 sec. Log inspection command: `sed -n '/test_read_service/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity count: 4 Tests, 0 Failures, 0 Ignored; OK. |
| US1 Write validation | FR-004, SC-001, SC-002, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(write_errors|write_decoder|write_service_integration)'` | Missing `DataValue` value rejected before callbacks; test count; failures. | PASS | T042g runner evidence: `cmake --build build/test --target test_write_errors test_write_decoder test_write_service_integration` passed with exit code 0; `ctest --test-dir build/test -R '^(test_write_errors|test_write_decoder|test_write_service_integration)$' --output-on-failure` passed with exit code 0, 3/3 tests passed, 0 failed, total real time 0.00 sec. Log inspection command: `sed -n '/test_write_decoder/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity counts: `test_write_decoder` 3 Tests, 0 Failures, 0 Ignored; `test_write_errors` 4 Tests, 0 Failures, 0 Ignored; `test_write_service_integration` 1 Tests, 0 Failures, 0 Ignored. |
| US1 Browse validation | FR-012, SC-001, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(browse_service|browse_limits)'` | Invalid BrowseDirection StatusCode; test count; failures. | PASS | T042i runner evidence: `cmake --build build/test --target test_browse_service test_browse_limits` passed with exit code 0; `ctest --test-dir build/test -R '^(test_browse_service|test_browse_limits)$' --output-on-failure` passed with exit code 0, 100% tests passed, 0 failed out of 2. Log inspection command: `sed -n '/test_browse_limits/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity counts: `test_browse_limits` 5 Tests, 0 Failures, 0 Ignored; `test_browse_service` 3 Tests, 0 Failures, 0 Ignored; total Browse Unity count 8 Tests, 0 Failures, 0 Ignored. |
| US1 Subscription validation | FR-002, FR-003, FR-013, SC-001, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(subscriptions_errors|subscriptions)'` | Filter body completeness and per-item StatusCodes; test count; failures. | PASS | T042k runner evidence: `cmake --build build/test --target test_subscriptions_errors test_subscriptions` passed with exit code 0; `ctest --test-dir build/test -R '^(test_subscriptions_errors|test_subscriptions)$' --output-on-failure` passed with exit code 0, 100% tests passed, 0 failed out of 2. Log inspection command: `sed -n '/test_subscriptions_errors/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity counts: `test_subscriptions_errors` 5 Tests, 0 Failures, 0 Ignored; `test_subscriptions` 20 Tests, 0 Failures, 0 Ignored. |
| US1 Query validation | FR-017, SC-001, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(query_service|query_encoding)'` | Overflow-safe count handling; capacity rejection result; test count; failures. | PASS | T042m runner evidence: `cmake --build build/test --target test_query_service test_query_encoding` passed; `ctest --test-dir build/test -R '^(test_query_service|test_query_encoding)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed. Log inspection command: `sed -n '/test_query_encoding/,/Test time/p; /test_query_service/,/Test time/p' build/test/Testing/Temporary/LastTest.log`. Unity counts: `test_query_encoding` 2 Tests, 0 Failures, 0 Ignored; `test_query_service` 4 Tests, 0 Failures, 0 Ignored. |
| US1 History validation | FR-002, FR-003, SC-001, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_history'` | Bounded HistoryRead details decode; test count; failures. | PASS | T042o runner evidence: `cmake --build build/test --target test_history` passed; `ctest --test-dir build/test -R '^test_history$' --output-on-failure` passed with exit code 0, 1/1 test passed, 0 failed, total test time 0.00 sec. Log inspection command: `sed -n '/test_history/,/Test time/p' build/test/Testing/Temporary/LastTest.log`. Unity count: 7 Tests, 0 Failures, 0 Ignored; OK. |
| US2 SecureChannel freshness | FR-005, SC-003, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(secure_channel|secure_handshake_modern)'` | Entropy-failure rejection; fresh token/nonce evidence; test count; failures. | PASS | T055a runner evidence: `cmake --build build/test --target test_secure_channel test_secure_handshake_modern` passed with exit code 0; `ctest --test-dir build/test -R '^(test_secure_channel|test_secure_handshake_modern)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed. The stale-token test fix parses the CreateSession `authenticationToken` instead of hardcoded `12345`, preserving entropy-backed token freshness evidence. |
| US2 Session binding | FR-005, FR-006, FR-007, SC-003, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(session|service_state_errors)'` | Fresh session identifiers; inactive vs invalid session StatusCodes; test count; failures. | PASS | T055c runner evidence: `cmake --build build/test --target test_session test_service_state_errors` passed with exit code 0; `ctest --test-dir build/test -R '^(test_session|test_service_state_errors)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed (`test_service_state_errors`, `test_session`). Coverage confirms fresh session identifiers plus inactive-vs-invalid session StatusCodes (`Bad_SessionNotActivated` vs `Bad_SessionIdInvalid`). |
| US2 Connection multiplexing | FR-006, SC-003, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(connection_multiplex|single_client_limit)'` | Cross-channel activation/use rejection; test count; failures. | PASS | T055e runner evidence: `cmake --build build/test --target test_connection_multiplex test_single_client_limit` passed with exit code 0; `ctest --test-dir build/test -R '^(test_connection_multiplex|test_single_client_limit)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed (`test_connection_multiplex`, `test_single_client_limit`). Coverage confirms cross-channel activation/use rejection plus the single-client limit. |
| US2 User authentication defaults | FR-008, SC-003, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(security_identity_errors|user_auth_plaintext)'` | Username/password over `SecurityPolicy#None` not advertised or accepted by default; test count; failures. | PASS | T055g runner evidence: `cmake --build build/test --target test_security_identity_errors test_user_auth_plaintext` passed with exit code 0; `ctest --test-dir build/test -R '^(test_security_identity_errors|test_user_auth_plaintext)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed (`test_security_identity_errors`, `test_user_auth_plaintext`). Coverage confirms username/password over `SecurityPolicy#None` is not advertised or accepted by default while anonymous-over-None remains valid. |
| US3 Discovery filters | FR-011, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(discovery_services|discovery_endpoint)'` | Endpoint URL, locale, server URI, server type, transport profile, and security-policy filter outcomes; test count; failures. | PASS | T074a runner evidence: `cmake --build build/test --target test_discovery_endpoint test_discovery_services` passed with exit code 0; `ctest --test-dir build/test -R '^(test_discovery_endpoint|test_discovery_services)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed. LastTest Unity counts: `test_discovery_endpoint` 6 Tests, 0 Failures, 0 Ignored; `test_discovery_services` 10 Tests, 0 Failures, 0 Ignored. |
| US3 MonitoringFilter semantics | FR-013, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_subscriptions_errors'` | Unsupported, invalid, and disallowed MonitoringFilter per-item StatusCodes; test count; failures. | PASS | T074c runner evidence: `cmake --build build/test --target test_subscriptions_errors` passed with exit code 0; `ctest --test-dir build/test -R '^test_subscriptions_errors$' --output-on-failure` passed with exit code 0, 1/1 tests passed, 0 failed. Log inspection command: `sed -n '/test_subscriptions_errors/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity count: `test_subscriptions_errors` 12 Tests, 0 Failures, 0 Ignored; OK. |
| US3 AggregateFilter scope | FR-014, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_aggregate'` | Average/Minimum/Maximum scoped support and unsupported aggregate rejection; test count; failures. | PASS | T074e runner evidence: `cmake --build build --target test_aggregate` passed with exit code 0; `ctest --test-dir build --output-on-failure -R 'test_aggregate'` passed with exit code 0, 1/1 tests passed, 0 failed. `build/Testing/Temporary/LastTest.log` showed 12 Tests, 0 Failures, 0 Ignored. Mirror `build/test` evidence: `cmake --build build/test --target test_aggregate` passed with exit code 0; `ctest --test-dir build/test -R '^test_aggregate$' --output-on-failure` passed with exit code 0, 1/1 tests passed, 0 failed. Log inspection command: `sed -n '/test_aggregate/,/End testing/p' build/test/Testing/Temporary/LastTest.log`; Unity count: 12 Tests, 0 Failures, 0 Ignored. |
| US3 Persistent data lifetime | FR-015, SC-002, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(query_service|history|node_management_errors|node_management)'` | Continuation-point and NodeManagement owned-storage stability; test count; failures. | PASS | T074g Query evidence: `cmake --build build/test --target test_query_service` passed with exit code 0; `ctest --test-dir build/test -R '^test_query_service$' --output-on-failure` passed with exit code 0, 1/1 CTest tests passed, 0 failed. Log inspection command: `sed -n '/test_query_service/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity count: `test_query_service` 5 Tests, 0 Failures, 0 Ignored, including `test_query_next_invalid_continuation_point_returns_bad_continuation_point_invalid`. T074i History evidence: `cmake --build build/test --target test_history` passed with exit code 0; `ctest --test-dir build/test -R '^test_history$' --output-on-failure` passed with exit code 0, 1/1 CTest tests passed, 0 failed. Log inspection command: `sed -n '/test_history/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity count: `test_history` 9 Tests, 0 Failures, 0 Ignored; OK. Coverage includes HistoryRead continuation-point ownership and later-request stability. T074k NodeManagement evidence: `cmake --build build/test --target test_node_management_errors test_node_management` passed with exit code 0; `ctest --show-only=json-v1 --test-dir build/test -R '^(test_node_management_errors|test_node_management)$'` passed with exit code 0 and confirmed both CTest names present/buildable; `ctest --test-dir build/test -R '^(test_node_management_errors|test_node_management)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed. Log inspection command: `sed -n '/test_node_management/,/End testing/p' build/test/Testing/Temporary/LastTest.log`. Unity counts: `test_node_management` 8 Tests, 0 Failures, 0 Ignored; `test_node_management_errors` 7 Tests, 0 Failures, 0 Ignored. Coverage includes BrowseName, DisplayName, string NodeId, and AddReferences target NodeId stability after request buffer overwrite. |
| US3 Publish oversize behavior | FR-016, SC-004 | `ctest --test-dir build --output-on-failure -R 'test_(subscriptions_errors|subscriptions)'` | Deterministic oversize Publish result; test count; failures. | PASS | T074m runner evidence: `cmake --build build/test --target test_subscriptions_errors test_subscriptions test_dispatch_services` passed with exit code 0; relevant row targets were `test_subscriptions_errors` and `test_subscriptions`. `ctest --test-dir build/test -R '^(test_subscriptions_errors|test_subscriptions)$' --output-on-failure` passed with exit code 0, 2/2 tests passed, 0 failed, total test time 0.00 sec (`test_subscriptions_errors`, `test_subscriptions`). LastTest evidence confirmed `test_publish_oversize_notification_response_returns_response_too_large_fault` passed in `test_subscriptions_errors`; Unity counts after the stale-fixture fix: `test_subscriptions_errors` 12 Tests, 0 Failures, 0 Ignored; `test_subscriptions` 20 Tests, 0 Failures, 0 Ignored. Coverage confirms deterministic oversize Publish returns `Bad_ResponseTooLarge` without regressing the broader subscription integration suite. |
| US3 Dispatch unsupported services | FR-012, FR-020, SC-004, SC-006 | `ctest --test-dir build --output-on-failure -R 'test_(dispatch_services|service_dispatch)'` | Unsupported service StatusCode; host gate regression result; test count; failures. | PASS | T074o runner evidence: `cmake --build build/test --target test_dispatch_services test_service_dispatch` passed with exit code 0; `ctest --show-only=json-v1 --test-dir build/test -R 'test_(dispatch_services|service_dispatch)'` passed with exit code 0 and matched both `test_dispatch_services` and `test_service_dispatch`; `ctest --test-dir build/test -R 'test_(dispatch_services|service_dispatch)' --output-on-failure` passed with exit code 0, 2/2 passed, 0 failed. LastTest log inspection command: `sed -n '/test_dispatch_services/,/End testing/p' build/test/Testing/Temporary/LastTest.log` passed with exit code 0. Unity counts: `test_dispatch_services` 15 Tests, 0 Failures, 0 Ignored; `test_service_dispatch` 4 Tests, 0 Failures, 0 Ignored; combined 19 Tests, 0 Failures, 0 Ignored. Unsupported service StatusCode coverage is present: `tests/unit/test_dispatch_services.c` asserts unsupported request id `437` returns `MU_STATUS_BAD_SERVICEUNSUPPORTED`, and `tests/unit/test_service_dispatch.c` asserts unsupported `CallRequest` and `HistoryReadRequest` return `MU_STATUS_BAD_SERVICEUNSUPPORTED`. |

## Sanitizer Validation Checklist

Coverage intent: this sanitizer gate supports FR-020 and SC-006 by requiring
sanitizer-enabled builds and CTest execution before release validation claims.
It also provides memory-safety evidence hooks for malformed-input, transport
boundary, security/session, filter, lifetime, and service-semantics coverage
under SC-001 through SC-004 as those tests are added.

T005b did not run these commands. T086b recorded the original sanitizer command
transcript on 2026-06-30 using `build-san` and the explicit ASan/UBSan flags
documented by `docs/validation/sanitizers.md`. That original run found the
HistoryRead continuation-point lifetime bug. T102b fixed that lifetime issue and
reran the sanitizer gates; the active ASan/UBSan status is PASS.

### Required Sanitizer Commands

| Check | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- |
| Sanitizer configure | `cmake -S . -B build-san -DMUC_OPCUA_BUILD_TESTS=ON -DMUC_OPCUA_SUBSCRIPTIONS_STANDARD=ON -DMUC_OPCUA_BASE_TYPE_SYSTEM=ON -DMU_MAX_MONITORED_ITEMS=100 -DMU_MAX_SUBSCRIPTIONS=2 -DMU_MAX_PUBLISH_REQUESTS=5 -DMU_MONITORED_QUEUE_DEPTH=2 -DMU_MAX_TRIGGER_LINKS=4 -DCMAKE_C_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer' -DCMAKE_EXE_LINKER_FLAGS='-fsanitize=address,undefined'` | Build dir; compiler; sanitizer flags in compile/link commands; enabled feature/profile knobs; CMake exit code; configure transcript path. | PASS | T086b sanitizer evidence: exit code `0`; build dir `build-san`; compiler `CMAKE_C_COMPILER=/usr/bin/cc`; `CMAKE_C_FLAGS=-fsanitize=address,undefined -fno-omit-frame-pointer`; `CMAKE_EXE_LINKER_FLAGS=-fsanitize=address,undefined`; enabled knobs `MUC_OPCUA_BUILD_TESTS=ON`, `MUC_OPCUA_SUBSCRIPTIONS_STANDARD=ON`, `MUC_OPCUA_BASE_TYPE_SYSTEM=ON`, `MU_MAX_MONITORED_ITEMS=100`, `MU_MAX_SUBSCRIPTIONS=2`, `MU_MAX_PUBLISH_REQUESTS=5`, `MU_MONITORED_QUEUE_DEPTH=2`, `MU_MAX_TRIGGER_LINKS=4`; transcript `build-san/validation-logs/020-audit-hardening-sanitizer-configure.log`. |
| Sanitizer build | `cmake --build build-san` | Build exit code; warnings/errors; produced `build-san/src/libmuc_opcua.a`; produced sanitizer-linked test executables; build transcript path. | PASS | T095a sanitizer build evidence remains valid: exit code `0`, sanitizer flags present, and 95 sanitizer-linked test executables produced. T102b reused this sanitizer build tree for the remediation reruns. |
| Sanitizer CTest suite | `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=print_stacktrace=1 ctest --test-dir build-san --output-on-failure` | CTest exit code; total/passed/failed/skipped test count; failing test names; ASan findings; UBSan findings; duration; `build-san/Testing/Temporary/LastTest.log` path. | PASS | T102b remediation evidence: the full ASan/UBSan CTest suite passed, exit code `0`, 95/95 CTest targets passed, total time approximately `4.71 sec`; no ASan or UBSan finding was reported. The earlier T096a `test_history` `stack-use-after-scope` finding is historical and fixed by owned continuation-point storage in `handle_history_read`. |
| Sanitizer evidence sync | `rg -n "020-audit-hardening|build-san|address,undefined|ASAN|UBSAN" docs/validation/sanitizers.md docs/validation/audit-hardening.md` | Cross-reference that this feature's sanitizer command/result is recorded in both validation ledgers when T086/T092/T095/T096 execute. | PASS | `docs/validation/sanitizers.md` records the T102b full-suite PASS and the historical T086b/T096a failure as pre-remediation context. |

### Focused Sanitizer Evidence Rows

Run these focused sanitizer commands only after the referenced tests exist or are
enabled in `build-san`. If a later task renames a test target, update the
relevant row before recording evidence.

| Evidence row | Coverage | Command | Expected result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Malformed decoding and transport bounds under ASan/UBSan | FR-001, FR-002, FR-003, FR-009, FR-010, SC-001, SC-002, SC-006 | `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=print_stacktrace=1 ctest --test-dir build-san --output-on-failure -R 'test_(binary_array_errors|binary_extension_object_errors|binary_string_errors|message_chunk_errors|tcp_connection|dispatch_services)'` | Exact rejection result; no state mutation; ASan/UBSan findings; test count; failures. | PASS | T086b focused sanitizer evidence: exit code `0`; 6/6 CTest targets passed, 0 failed, total test time `0.01 sec`; no ASan/UBSan findings recorded for this focused group; transcript `build-san/validation-logs/020-audit-hardening-sanitizer-focused-malformed.log`. This is focused coverage only and does not make the full sanitizer gate pass. |
| Security and session misuse under ASan/UBSan | FR-005, FR-006, FR-007, FR-008, SC-003, SC-004, SC-006 | `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=print_stacktrace=1 ctest --test-dir build-san --output-on-failure -R 'test_(secure_channel|session|connection_multiplex|service_state_errors|security_identity_errors|user_auth_plaintext)'` | Freshness failure behavior; channel-binding rejection; identity-token policy result; ASan/UBSan findings; test count; failures. | PASS | T086b focused sanitizer evidence: exit code `0`; 7/7 CTest targets passed, 0 failed, total test time `0.02 sec`; no ASan/UBSan findings recorded for this focused group; transcript `build-san/validation-logs/020-audit-hardening-sanitizer-focused-security-session.log`. This is focused coverage only and does not make the full sanitizer gate pass. |
| Optional service lifetime and filter paths under ASan/UBSan | FR-013, FR-014, FR-015, FR-016, FR-017, SC-002, SC-004, SC-006 | `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=print_stacktrace=1 ctest --test-dir build-san --output-on-failure -R 'test_(subscriptions_errors|aggregate|query_service|history|node_management_errors|node_management)'` | Filter StatusCodes; owned-storage stability; oversize Publish behavior; ASan/UBSan findings; test count; failures. | PASS | T102b focused HistoryRead evidence: `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=print_stacktrace=1 ctest --test-dir build-san -R '^test_history$' --output-on-failure` passed, 1/1 CTest target, after the continuation-point lifetime fix. The full sanitizer suite also passed 95/95, covering this row's prior failing path. |

## Fuzz Validation Checklist

Coverage intent: this fuzz gate supports FR-020 and SC-006 by requiring
meaningful libFuzzer harnesses for malformed-input and transport-boundary
regression coverage before release validation claims. The active feature rows
also provide evidence hooks for FR-001, FR-003, and FR-010 through T022a/T022b
and T023a-T023d, with related hooks for security identity and filter parsing
under FR-008, FR-013, and FR-017.

T005c did not run these commands. Fuzzing requires Clang/libFuzzer in this
project; if the toolchain is unavailable, record the configure or build result
as blocked with the compiler/version evidence rather than as a pass. Short
smoke commands are not a substitute for later fuzz campaigns.

T087a-T087e recorded the original Feature 020 fuzz evidence on 2026-06-30 in
`docs/validation/fuzz.md`. T102b replaced the former `fuzz_binary_types`
placeholder/input-discard harness with input-consuming binary type coverage and
reran the focused smoke. The active fuzz-placeholder blocker is closed; longer
campaigns remain a separate quality improvement beyond this blocker row.

### Required Fuzz Commands

| Check | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- |
| Fuzz harness placeholder scan | `rg -n "To be implemented|TODO|\\(void\\)data;|\\(void\\)size;" tests/fuzz` | Match count; matching file/line list; intentional placeholders tied to follow-up tasks; unexpected placeholders fixed before validation. | PASS | T102b refresh: the former `tests/fuzz/fuzz_binary_types.c` placeholder/input-discard markers were removed. The focused scan of `tests/fuzz/fuzz_binary_types.c` is expected to return no matches, exit code `1`, and `test_conformance_docs` passed after the replacement. |
| Fuzz configure | `cmake -S . -B build/fuzz -DMUC_OPCUA_BUILD_FUZZERS=ON -DMUC_OPCUA_SUBSCRIPTIONS_STANDARD=ON -DMUC_OPCUA_BASE_TYPE_SYSTEM=ON -DMU_MAX_MONITORED_ITEMS=100 -DMU_MAX_SUBSCRIPTIONS=2 -DMU_MAX_PUBLISH_REQUESTS=5 -DMU_MONITORED_QUEUE_DEPTH=2 -DMU_MAX_TRIGGER_LINKS=4 -DCMAKE_C_COMPILER=clang` | Build dir; Clang version; libFuzzer support; enabled feature/profile knobs; CMake exit code; configure transcript path; any "Fuzzing requires Clang" warning. | PASS | T087a-T087e reran the same configure command for the focused fuzz targets; each configure exited `0` and wrote build files to `build/fuzz` with Ubuntu clang `18.1.3` available. Transcripts: `/tmp/muc-opcua-t087a-fuzz-configure.log`, `/tmp/muc-opcua-t087b-fuzz-configure.log`, `/tmp/muc-opcua-t087c-fuzz-configure.log`, `/tmp/muc-opcua-t087d-fuzz-configure.log`, `/tmp/muc-opcua-t087e-fuzz-configure.log`. |
| Fuzz target build | `cmake --build build/fuzz` | Build exit code; warnings/errors; produced `build/fuzz/tests/fuzz/fuzz_binary_reader`, `fuzz_message_chunk`, `fuzz_binary_types`, `fuzz_activate_session`, `fuzz_subscription_filters`, and `fuzz_call`; build transcript path. | PARTIAL | The aggregate build command was not recorded by T087a-T087e. Focused target builds all exited `0` and produced `build/fuzz/tests/fuzz/fuzz_binary_reader`, `build/fuzz/tests/fuzz/fuzz_message_chunk`, `build/fuzz/tests/fuzz/fuzz_activate_session`, `build/fuzz/tests/fuzz/fuzz_binary_types`, and `build/fuzz/tests/fuzz/fuzz_subscription_filters`. Transcripts: `/tmp/muc-opcua-t087a-fuzz-build.log`, `/tmp/muc-opcua-t087b-fuzz-message-chunk-build.log`, `/tmp/muc-opcua-t087c-fuzz-activate-session-build.log`, `/tmp/muc-opcua-t087d-fuzz-binary-types-build.log`, `/tmp/muc-opcua-t087e-fuzz-subscription-filters-build.log`. No T087 aggregate build transcript or current `fuzz_call` build transcript was recorded, so this row does not close the full build gate. |
| Fuzz target inventory | `cmake --build build/fuzz --target help` | Generated target list includes `fuzz_binary_reader`, `fuzz_message_chunk`, `fuzz_binary_types`, `fuzz_activate_session`, `fuzz_subscription_filters`, and `fuzz_call`; missing or renamed targets; target-to-source mapping from `tests/fuzz/CMakeLists.txt`. | PARTIAL | T087a-T087e did not record this inventory command. `docs/validation/fuzz.md` records source mapping from `tests/fuzz/CMakeLists.txt` for generated targets `fuzz_binary_reader`, `fuzz_message_chunk`, `fuzz_activate_session`, `fuzz_binary_types`, and `fuzz_subscription_filters`; it does not record a current T087 inventory transcript or current `fuzz_call` target mapping. |
| Fuzz smoke loop | `for target in fuzz_binary_reader fuzz_message_chunk fuzz_binary_types fuzz_activate_session fuzz_subscription_filters fuzz_call; do build/fuzz/tests/fuzz/$target -runs=1; done` | Per-target exit code; stderr/stdout transcript; crash, timeout, OOM, or sanitizer finding status; generated repro artifact path if any. | PASS for required blocker target | T102b focused blocker evidence: `cmake --build build-fuzz --target fuzz_binary_types` passed and `build-fuzz/tests/fuzz/fuzz_binary_types -runs=2` passed with no crash, timeout, OOM, or sanitizer finding and non-startup coverage (`cov: 11`, `ft: 12`). The aggregate all-target loop remains useful follow-up evidence, but the release-blocking placeholder condition is cleared. |
| Fuzz fixed-input smokes | `build/fuzz/tests/fuzz/fuzz_message_chunk -runs=1 tests/fixtures/uasc/msg_chunk.bin && build/fuzz/tests/fuzz/fuzz_call -runs=1 tests/fixtures/embedded/call-get-monitored-items.hex` | Fixture paths; per-target exit code; fixed-input transcript; crash/reproducer path if any. | PARTIAL | T087b fixed-input smoke `build/fuzz/tests/fuzz/fuzz_message_chunk -runs=1 tests/fixtures/uasc/msg_chunk.bin` exited `0`; libFuzzer executed the fixture once with no crash, timeout, OOM, or sanitizer finding and noted fixed-input execution is not fuzzing. Transcript `/tmp/muc-opcua-t087b-fuzz-message-chunk-fixed-input.log`. T087c also recorded `build/fuzz/tests/fuzz/fuzz_activate_session -runs=1 tests/fixtures/services/activatesession_req.bin` exit code `0`, transcript `/tmp/muc-opcua-t087c-fuzz-activate-session-fixed-input.log`. No current T087 `fuzz_call` fixed-input run was recorded, so this required row remains partial. |
| Fuzz evidence sync | `rg -n "020-audit-hardening|T022|T023|fuzz_binary_reader|fuzz_message_chunk|build/fuzz" docs/validation/fuzz.md docs/validation/audit-hardening.md` | Cross-reference that this feature's fuzz command/result is recorded in both validation ledgers when T087/T097 execute. | PASS | `docs/validation/fuzz.md` and this ledger both record the current T102b `fuzz_binary_types` remediation and the earlier passing smokes for the other fuzz targets. |

### Active Feature Fuzz Evidence Rows

Run these focused fuzz commands after the referenced harness changes exist. If a
later task renames a target, updates seed fixtures, or moves the fuzz build dir,
update the relevant row before recording evidence.

| Evidence row | Coverage | Command | Expected result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Binary reader malformed-array and length boundaries | FR-001, FR-003, FR-020, SC-001, SC-006; T022a/T022b; OPC-10000-6 sections 5.2 and 5.2.5 | `build/fuzz/tests/fuzz/fuzz_binary_reader -runs=1` | Harness includes array counts below `-1` and declared-length boundary paths; no crash, timeout, OOM, or sanitizer finding; run count and transcript path. | PASS | T087a evidence: configure exit code `0`, build target exit code `0`, produced `build/fuzz/tests/fuzz/fuzz_binary_reader`; smoke exit code `0`, libFuzzer completed `Done 2 runs in 0 second(s)` with no crash, timeout, OOM, or sanitizer finding. Transcripts: `/tmp/muc-opcua-t087a-fuzz-configure.log`, `/tmp/muc-opcua-t087a-fuzz-build.log`, `/tmp/muc-opcua-t087a-fuzz-binary-reader-smoke.log`. |
| MessageChunk invalid type, abort, finality, and negotiated-size boundaries | FR-003, FR-010, FR-020, SC-001, SC-006; T023a-T023d; OPC-10000-6 sections 6.7.2 and 7.1.2.2 | `build/fuzz/tests/fuzz/fuzz_message_chunk -runs=1 tests/fixtures/uasc/msg_chunk.bin` | Harness covers invalid message types, abort chunks, non-final chunks, and negotiated-size boundaries; no service-body dispatch from invalid chunks; no crash, timeout, OOM, or sanitizer finding. | PASS | T087b evidence: configure exit code `0`, build target exit code `0`, produced `build/fuzz/tests/fuzz/fuzz_message_chunk`; unfixed-input smoke `build/fuzz/tests/fuzz/fuzz_message_chunk -runs=1` exit code `0`; fixed-input smoke `build/fuzz/tests/fuzz/fuzz_message_chunk -runs=1 tests/fixtures/uasc/msg_chunk.bin` exit code `0`; no crash, timeout, OOM, or sanitizer finding reported. Transcripts: `/tmp/muc-opcua-t087b-fuzz-configure.log`, `/tmp/muc-opcua-t087b-fuzz-message-chunk-build.log`, `/tmp/muc-opcua-t087b-fuzz-message-chunk-smoke.log`, `/tmp/muc-opcua-t087b-fuzz-message-chunk-fixed-input.log`. |
| Binary type decoder boundaries | FR-001, FR-003, FR-020, SC-001, SC-006; OPC-10000-6 sections 5.2, 5.2.2.15, and 5.2.5 | `build-fuzz/tests/fuzz/fuzz_binary_types -runs=2` | NodeId, String, ByteString, ExtensionObject, Variant, DataValue, and array decode surfaces exercised; no crash, timeout, OOM, or sanitizer finding. | PASS | T102b evidence: `tests/fuzz/fuzz_binary_types.c` now consumes fuzz input and exercises binary type decode surfaces. `cmake --build build-fuzz --target fuzz_binary_types` passed, and `build-fuzz/tests/fuzz/fuzz_binary_types -runs=2` passed with no crash, timeout, OOM, or sanitizer finding and non-startup coverage (`cov: 11`, `ft: 12`). |
| ActivateSession identity-token boundaries | FR-003, FR-005, FR-008, FR-020, SC-003, SC-006; OPC-10000-4 sections 5.7.3 and 7.40.2.1; OPC-10000-6 section 5.2.2.15 | `build/fuzz/tests/fuzz/fuzz_activate_session -runs=1 tests/fixtures/services/activatesession_req.bin` | Identity ExtensionObject and credential-policy decode paths exercised; no plaintext username/password-over-None acceptance claim without matching host evidence; no crash, timeout, OOM, or sanitizer finding. | PASS | T087c evidence: configure exit code `0`, build target exit code `0`, produced `build/fuzz/tests/fuzz/fuzz_activate_session`; smoke `build/fuzz/tests/fuzz/fuzz_activate_session -runs=1` exit code `0`; fixed-input smoke `build/fuzz/tests/fuzz/fuzz_activate_session -runs=1 tests/fixtures/services/activatesession_req.bin` exit code `0`; no crash, timeout, OOM, or sanitizer finding reported. Transcripts: `/tmp/muc-opcua-t087c-fuzz-configure.log`, `/tmp/muc-opcua-t087c-fuzz-activate-session-build.log`, `/tmp/muc-opcua-t087c-fuzz-activate-session-smoke.log`, `/tmp/muc-opcua-t087c-fuzz-activate-session-fixed-input.log`. |
| Subscription filter decoder boundaries | FR-013, FR-017, FR-020, SC-004, SC-006; OPC-10000-4 sections 5.13.2.4, 5.13.3.4, 7.7.1, and 7.22.1 | `build/fuzz/tests/fuzz/fuzz_subscription_filters -runs=1` | MonitoringFilter and operand count paths exercised; unsupported, invalid, or disallowed filters remain evidence-linked to exact StatusCode host tests; no crash, timeout, OOM, or sanitizer finding. | PASS | T087e evidence: configure exit code `0`, build target exit code `0`, produced `build/fuzz/tests/fuzz/fuzz_subscription_filters`; smoke exit code `0`, libFuzzer completed `Done 2 runs in 0 second(s)` with no crash, timeout, OOM, or sanitizer finding and reported `cov: 8`, `ft: 9`, `corp: 1/1b`, `rss: 29Mb`. Transcripts: `/tmp/muc-opcua-t087e-fuzz-configure.log`, `/tmp/muc-opcua-t087e-fuzz-subscription-filters-build.log`, `/tmp/muc-opcua-t087e-fuzz-subscription-filters-smoke.log`. |
| Optional dispatch fuzz target smoke | FR-020, SC-006 | `build/fuzz/tests/fuzz/fuzz_call -runs=1 tests/fixtures/embedded/call-get-monitored-items.hex` | Existing optional dispatch target still builds and smokes after audit-hardening changes; no crash, timeout, OOM, or sanitizer finding. | TBD | TBD |

## Embedded-Build Validation Checklist

Coverage intent: this embedded-build gate supports FR-020 and SC-006 by
requiring embedded/profile build readiness checks before release validation
claims. The resource rows also provide later evidence hooks for FR-019 and
SC-007, and the profile rows keep OPC-001, FR-018, and SC-005 scoped to
profile-targeting evidence rather than profile-compliant or CTT-verified claims.

T005d did not run these commands. Record configure output, build output,
toolchain versions, profile knobs, artifact paths, size/stack deltas, duration,
and log paths when the later run/record tasks execute. Do not treat prior
results in `docs/validation/pico-cross-compile.md` or
`docs/validation/arduino-platformio.md` as audit-hardening pass evidence unless
the relevant command is rerun for this feature.

### Required Embedded/Profile Commands

| Check | Coverage | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Host profile matrix | FR-020, SC-006, SC-007 | `make all-profiles` | Per-profile exit code for `nano`, `micro`, and `embedded`; build dirs `build/nano`, `build/micro`, and `build/embedded`; produced `examples/minimal_server` binaries; warnings/errors; transcript path. | TBD | TBD |
| Embedded profile host build | FR-019, FR-020, SC-006, SC-007 | `make embedded` | Confirm `MUC_OPCUA_PROFILE=embedded`, `MUC_OPCUA_OPTIMIZE_SIZE=ON`, `MU_MAX_SUBSCRIPTIONS=2`, `MU_MAX_MONITORED_ITEMS=100`, `MU_MAX_PUBLISH_REQUESTS=5`, `MU_MONITORED_QUEUE_DEPTH=2`, and `MU_MAX_TRIGGER_LINKS=4`; produced `build/embedded/src/libmuc_opcua.a`; produced `build/embedded/examples/minimal_server`; exit code; transcript path. | TBD | TBD |
| Optional-service build matrix | FR-020, SC-006 | `BUILD_DIR=build/audit-matrix scripts/check_build_matrix.sh` | Discovered optional toggles; per-toggle configure/build result; symbol-absence check result or warning; overall matrix status; CMake and `nm` versions; transcript path. | PASS | T102b evidence: `BUILD_DIR=build/audit-matrix scripts/check_build_matrix.sh` passed for all 10 discovered optional-service configurations. The former `discovery.c` unused `policy` blocker is fixed. The script still records warnings for toggles whose disabled-symbol mapping is advisory (`WRITE`, `HISTORY`, `QUERY`, `NODEMANAGEMENT`). |
| Pico configure | OPC-001, FR-020, SC-006, SC-007 | `cmake -S . -B build/audit-pico -DMUC_OPCUA_PLATFORM=pico -DPICO_SDK_FETCH_FROM_GIT=ON -DMUC_OPCUA_PROFILE=embedded -DMU_MAX_SUBSCRIPTIONS=2 -DMU_MAX_MONITORED_ITEMS=100 -DMU_MAX_PUBLISH_REQUESTS=5 -DMU_MONITORED_QUEUE_DEPTH=2 -DMU_MAX_TRIGGER_LINKS=4 -DMUC_OPCUA_BUILD_EXAMPLES=ON` | CMake exit code; Pico SDK source path or fetched revision; `PICO_PLATFORM=rp2040`; `PICO_BOARD=pico`; `PICO_GCC_TRIPLE`; `arm-none-eabi-gcc` version; profile/capacity knobs; configure transcript path. | PASS | Current embedded-profile Pico build tree `build/t089a-pico-embedded` is configured with `MUC_OPCUA_PROFILE=embedded`, `MUC_OPCUA_OPTIMIZE_SIZE=ON`, `PICO_PLATFORM=rp2040`, `PICO_BOARD=pico`, `PICO_GCC_TRIPLE=arm-none-eabi`, and `CMAKE_C_COMPILER=/usr/bin/arm-none-eabi-gcc`. |
| Pico build | OPC-001, FR-020, SC-006, SC-007 | `cmake --build build/audit-pico` | Build exit code; warnings/errors; produced `build/audit-pico/src/libmuc_opcua.a`; produced `build/audit-pico/platform/pico/pico_minimal_server.elf`; produced `build/audit-pico/platform/pico/pico_minimal_server.uf2`; build transcript path. | PASS | T102b evidence: `cmake --build build/t089a-pico-embedded` passed after increasing `MU_CONNECTION_BASE_STORAGE_BYTES` to cover connection storage outside `rx_buffer`. Produced `build/t089a-pico-embedded/src/libmuc_opcua.a` (`703,784` bytes), `build/t089a-pico-embedded/platform/pico/pico_minimal_server.elf` (`1,014,188` bytes), and `build/t089a-pico-embedded/platform/pico/pico_minimal_server.uf2` (`138,752` bytes). `arm-none-eabi-size` for the ELF reported `text=73,428`, `data=0`, `bss=119,340`, `dec=192,768`. |
| Arduino/PlatformIO skeleton | FR-020, SC-006 | `platformio run -d platform/arduino -e muc_opcua_arduino` | PlatformIO version; environment `muc_opcua_arduino`; board `uno`; framework `arduino`; adapter compile result; artifact directory under `platform/arduino/.pio/build/muc_opcua_arduino`; warnings/errors; transcript path. | TBD | TBD |

### Embedded Resource Evidence Rows

Run these resource commands after the embedded/profile builds are expected to be
stable. If a later task changes the build directory, threshold, profile names,
or size methodology, update the row before recording evidence.

| Evidence row | Coverage | Command | Expected result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| ARM Cortex-M0+ profile size matrix | FR-019, SC-007 | `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh all`; `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh nano`; `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh embedded` | `arm-none-eabi-gcc` and `arm-none-eabi-size` versions; per-profile `text`, `data`, `bss`, and `dec` totals for `nano`, `micro`, `embedded`, and `full-featured`; archive paths under `build/audit-size-arm`; comparison against `docs/size/audit-hardening-baseline.md` and the plan budget. | PASS | T102b evidence: `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh all` passed. Current ARM Cortex-M0+ totals are nano `text=16,366 data=0 bss=0 dec=16,366`, micro `23,873/0/0/23,873`, embedded `43,078/0/0/43,078`, and full-featured `51,172/0/0/51,172`. The former nano unused-variable and embedded connection-storage assertion blockers are fixed. |
| Embedded stack-usage configure | FR-019, SC-007 | `cmake -S . -B build/audit-embedded-stack -DMUC_OPCUA_BUILD_TESTS=ON -DMUC_OPCUA_OPTIMIZE_SIZE=ON -DMUC_OPCUA_STACK_USAGE=ON -DMUC_OPCUA_STACK_USAGE_LIMIT=10240 -DMUC_OPCUA_PROFILE=embedded -DMU_MAX_SUBSCRIPTIONS=2 -DMU_MAX_MONITORED_ITEMS=100 -DMU_MAX_PUBLISH_REQUESTS=5 -DMU_MONITORED_QUEUE_DEPTH=2 -DMU_MAX_TRIGGER_LINKS=4` | CMake exit code; compiler; stack-usage instrumentation enabled; threshold value; embedded profile/capacity knobs; configure transcript path. | RECORDED | T088d stack evidence uses existing `build/audit-embedded-stack/CMakeCache.txt`: `MUC_OPCUA_PROFILE=embedded`, `MUC_OPCUA_OPTIMIZE_SIZE=ON`, `MUC_OPCUA_STACK_USAGE=ON`, `MUC_OPCUA_STACK_USAGE_LIMIT=10240`, `MUC_OPCUA_PLATFORM=host`, `CMAKE_BUILD_TYPE=Release`, compiler `/usr/bin/cc`, security/subscriptions/multiple connections enabled, and embedded capacity overrides. This is host stack instrumentation evidence, not Pico stack evidence. |
| Embedded stack-usage gate | FR-019, SC-007 | `cmake --build build/audit-embedded-stack && bash scripts/check_stack_usage.sh --build-dir build/audit-embedded-stack --threshold 10240` | Build exit code; generated `.su` files; secured OPN stack estimate; threshold; pass/fail result; comparison against `docs/size/audit-hardening-baseline.md`; transcript path. | PASS | T102b evidence: `cmake --build build/audit-embedded-stack` and `bash scripts/check_stack_usage.sh --build-dir build/audit-embedded-stack --threshold 10240` passed. The host secured OPN stack estimate is `3,040 B`, threshold `10,240 B`, matching the baseline estimate. |
| Pico stack-usage gate | FR-019, SC-007 | `cmake --build build/t089b-pico-stack && bash scripts/check_stack_usage.sh --build-dir build/t089b-pico-stack --threshold 10240` | Build exit code; generated `.su` files; secured OPN stack estimate; required frames; threshold; Pico ELF/UF2 artifact status; transcript path. | PASS | T102b evidence: `cmake --build build/t089b-pico-stack` and `bash scripts/check_stack_usage.sh --build-dir build/t089b-pico-stack --threshold 10240` passed. Pico stack artifacts include 35 `.su` files. Secured OPN stack estimate is `2,776 B` against the `10,240 B` threshold; required frames include asymmetric unwrap/wrap, dispatch, OpenSecureChannel, symmetric key derivation, and `mu_p_sha256`. |

## Size-Budget Validation Checklist

Coverage intent: this size-budget gate supports FR-019, FR-020, and SC-007.
It also records the active feature's application-headroom budget from
`specs/020-audit-hardening/plan.md`: host/full-profile `.text` growth under
+8 KiB, nano/embedded `.text` growth under +4 KiB, no new default static RAM
beyond existing server/session/channel storage, no new protocol hot-path heap,
and recorded stack evidence for hardening changes.

T005f did not run these commands. T092f recorded the original size/resource
status on 2026-06-30 from T088/T089 evidence. T102b fixed the build blockers
and changed the default host/full build to optimize for size; the active
resource blocker rows now have current evidence. This does not create external
OPC Foundation CTT evidence.

### Required Size Commands

| Check | Coverage | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Baseline lookup | FR-019, SC-007 | `sed -n '1,170p' docs/size/audit-hardening-baseline.md` | Baseline source file; pre-change host archive `text=152709`, `data=6224`, `bss=0`; pre-change stack estimate `3040 bytes`; stack threshold `10240 bytes`; any missing ARM profile baseline rows to resolve before claiming SC-007. | RECORDED / CURRENT ARTIFACTS AVAILABLE | T092f baseline review confirms `docs/size/audit-hardening-baseline.md` records host archive baseline `text=152709`, `data=6224`, `bss=0`, stack baseline `3040 bytes`, and stack threshold `10240 bytes`. T102b adds current ARM/Pico artifacts and absolute size rows; no missing ARM baseline is used to invent unrecorded deltas. |
| Host/full size report | FR-019, FR-020, SC-007 | `cmake --build build && ./scripts/size-report.sh build/src/libmuc_opcua.a`; `size -t build/src/libmuc_opcua.a` | Build exit code; produced `build/src/libmuc_opcua.a`; `size` tool version if recorded; post-change `text`, `data`, `bss`, `dec`; transcript path; delta versus `docs/size/audit-hardening-baseline.md`. | PASS | T102b evidence: the default full-profile host build now uses `MUC_OPCUA_OPTIMIZE_SIZE=ON`. Clean full build `build/audit-host-size-default` reports `text=96,398`, `data=6,224`, `bss=0`, `dec=102,622`; delta versus baseline `text=152,709` is `-56,311 B`, so the +8 KiB budget passes. Explicit opt-out `MUC_OPCUA_OPTIMIZE_SIZE=OFF` remains a reference build at `text=166,784`, not the constrained-device release default. |
| ARM profile size matrix | FR-019, FR-020, SC-007 | `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh all` | `arm-none-eabi-gcc` and `arm-none-eabi-size` versions; per-profile archive path; `nano`, `micro`, `embedded`, and `full-featured` `text`, `data`, `bss`, and `dec`; baseline source for each profile; deltas versus baseline; transcript path. | PASS | T102b evidence: `BUILD_ROOT=build/audit-size-arm scripts/measure_size.sh all` passed with nano `16,366/0/0/16,366`, micro `23,873/0/0/23,873`, embedded `43,078/0/0/43,078`, and full-featured `51,172/0/0/51,172`. |
| Static RAM symbol review | FR-019, SC-007 | `nm -S --size-sort build/src/libmuc_opcua.a | rg ' [BbDd] '` | `.data`/`.bss` symbol list; new default mutable storage, if any; owning feature macro for optional storage; `MU_*_STORAGE_BYTES` impact; review notes for expected existing storage. | PASS / EVIDENCE RECORDED | T102b evidence: host/full archive `.data=6,224 B` and `.bss=0 B` are flat versus baseline; `nm -S --size-sort build/src/libmuc_opcua.a \| rg ' [BbDd] '` reports only initialized data symbols and no BSS. Caller-provided absolute storage is recorded rather than inventing a missing pre-change delta: host/full `sizeof(struct mu_server)=116,040`, `MU_SERVER_STORAGE_BYTES=125,020`, `sizeof(mu_connection_t)=8,560`, `MU_CONNECTION_RX_BUFFER_SIZE=8,192`, `MU_CONNECTION_BASE_STORAGE_BYTES=1,328`; ARM embedded `sizeof(struct mu_server)=85,040`, `MU_SERVER_STORAGE_BYTES=98,520`, `sizeof(mu_connection_t)=9,480`; ARM nano `sizeof(struct mu_server)=664`, `MU_SERVER_STORAGE_BYTES=1,280`. |
| Heap-use review | FR-019, SC-007 | `rg -n "\\b(malloc|calloc|realloc|free)\\b" src include platform examples`; `rg -n 'malloc|calloc|realloc|free\\(|OPENSSL_malloc' src include tests` | Match count; file/line list; classification as protocol hot path, optional platform/backend path, or example-only path; confirmation that no new mandatory protocol hot-path heap is introduced. | PASS for core protocol hot path | T102b evidence: `rg -n 'malloc|calloc|realloc|free\\(|OPENSSL_malloc' src/core src/encoding src/services src/security platform include` found no core protocol hot-path allocator calls. Remaining matches are `mu_session_find_free` false positives and the `cipher_ctx_free` adapter callback in `src/security/sym_chunk.c`, plus platform/backend responsibilities outside the core hot-path allocation claim. |
| Stack-usage configure | FR-019, SC-007 | `cmake -S . -B build/audit-embedded-stack -DMUC_OPCUA_BUILD_TESTS=ON -DMUC_OPCUA_OPTIMIZE_SIZE=ON -DMUC_OPCUA_STACK_USAGE=ON -DMUC_OPCUA_STACK_USAGE_LIMIT=10240 -DMUC_OPCUA_PROFILE=embedded -DMU_MAX_SUBSCRIPTIONS=2 -DMU_MAX_MONITORED_ITEMS=100 -DMU_MAX_PUBLISH_REQUESTS=5 -DMU_MONITORED_QUEUE_DEPTH=2 -DMU_MAX_TRIGGER_LINKS=4` | CMake exit code; compiler; `-fstack-usage` instrumentation enabled; embedded profile/capacity knobs; threshold value; configure transcript path. | RECORDED | T088d stack evidence records host stack instrumentation in `build/audit-embedded-stack`. T102b separately records the current Pico stack build/check pass in the Pico stack row. |
| Stack-usage comparison | FR-019, SC-007 | `cmake --build build/audit-embedded-stack && bash scripts/check_stack_usage.sh --build-dir build/audit-embedded-stack --threshold 10240` | Build exit code; generated `.su` files; secured OPN stack estimate; frames and phase breakdown; threshold result; delta versus `3040 bytes` in `docs/size/audit-hardening-baseline.md`; transcript path. | PASS | T102b evidence: host stack check passes at `3,040 B` against `10,240 B`; Pico stack check passes at `2,776 B` against `10,240 B` with 35 `.su` files. |
| Size-document sync | FR-019, FR-020, SC-007 | `rg -n "020-audit-hardening|SC-007|\\+8 KiB|\\+4 KiB|MU_[A-Z0-9_]*STORAGE_BYTES|heap|stack" docs/size/feature-size-ledger.md docs/size/host-minimal-server.md docs/size/pico-minimal-server.md docs/validation/audit-hardening.md` | Evidence that final size, RAM, heap, and stack results are cross-referenced in the size docs and this validation ledger; missing or stale rows to update. | PASS / RECORDED | T102b sync records current host/full, ARM, Pico, static-RAM, heap, and stack evidence in `docs/size/feature-size-ledger.md`, `docs/size/pico-minimal-server.md`, and this ledger. |

### Budget Comparison Worksheet

Fill these rows from the required command outputs. A larger increase is not a
pass; it requires explicit rationale tied to a High/P0/P1 audit finding and
must be recorded in the evidence field.

| Budget item | Required comparison | Threshold | Status | Evidence |
| --- | --- | --- | --- | --- |
| Host/full `.text` growth | Compare post-change `./scripts/size-report.sh build/src/libmuc_opcua.a` `text` against `152709` from `docs/size/audit-hardening-baseline.md`; also record the `full-featured` row from `scripts/measure_size.sh all` when ARM baseline evidence is available. | Delta `< 8192` bytes (+8 KiB). | PASS | T102b default optimized host/full evidence: `text=96,398`, `data=6,224`, `bss=0`, `dec=102,622`; delta vs baseline `text=152,709` is `-56,311 B`, below the +8 KiB budget. Explicit `MUC_OPCUA_OPTIMIZE_SIZE=OFF` opt-out remains a reference-only over-budget build at `text=166,784`. |
| Nano `.text` growth | Compare post-change `nano` `text` from `scripts/measure_size.sh all` against the recorded pre-change nano baseline source; cite `docs/size/audit-hardening-baseline.md` or record the baseline-source gap before claiming SC-007. | Delta `< 4096` bytes (+4 KiB). | PASS / CURRENT VALUE RECORDED | T102b ARM matrix evidence: nano `text=16,366`, `data=0`, `bss=0`, `dec=16,366`. This is below the historical nano values already recorded in this ledger (`16,441 B` feature-011 summary) and clears the prior build blocker. |
| Embedded `.text` growth | Compare post-change `embedded` `text` from `scripts/measure_size.sh all` against the recorded pre-change embedded baseline source; cite `docs/size/audit-hardening-baseline.md` or record the baseline-source gap before claiming SC-007. | Delta `< 4096` bytes (+4 KiB). | PASS / CURRENT VALUE RECORDED | T102b ARM matrix evidence: embedded `text=43,078`, `data=0`, `bss=0`, `dec=43,078`; Pico embedded build also produces ELF/UF2 artifacts. The prior `MU_CONNECTION_BASE_STORAGE_BYTES` static assertion blocker is fixed. |
| Default static RAM | Compare post-change `data`, `bss`, and `nm` default-storage review against the baseline host archive `data=6224`, `bss=0` and the storage notes in `docs/size/feature-size-ledger.md`. | No new default static RAM beyond existing server/session/channel storage; optional storage must be feature-macro gated and update `MU_*_STORAGE_BYTES` evidence. | PASS / ABSOLUTE CALLER STORAGE RECORDED | Host/full archive remains `data=6,224`, `bss=0`, flat against baseline; ARM archives report `data=0`, `bss=0`. Caller-provided storage absolute values are recorded, including the updated `MU_CONNECTION_BASE_STORAGE_BYTES=1,328`; no unbaselined delta is invented. |
| Protocol hot-path heap | Review heap-use grep matches and any platform crypto/backend exceptions. | No new mandatory heap allocation in the protocol hot path. | PASS for core protocol hot path | T102b source review found no core protocol hot-path allocator calls under `src/core`, `src/encoding`, or `src/services`; remaining matches are false positives or platform/backend adapter hooks outside the core hot-path claim. |
| Stack evidence | Compare post-change `check_stack_usage.sh` estimate against the pre-change `3040 bytes` baseline and record the `10240 bytes` threshold result. | Stack evidence recorded; threshold result and delta documented. | PASS | Host stack check passes at `3,040 B` against `10,240 B`; Pico stack check passes at `2,776 B` against `10,240 B` with 35 `.su` files. |

## Conformance-Doc Validation Checklist

Coverage intent: this conformance-documentation gate supports FR-018,
FR-019, FR-020, SC-005, and SC-006. It also provides the validation hook for
OPC-001, OPC-007, OPC-008, and OPC-009 by checking that public conformance
claims stay profile-targeting until external CTT evidence exists, every
protocol claim cites exact OPC UA sections, StatusCode documentation remains
numeric and traceable, and traceability placeholders are removed before
closure.

T005e did not run these commands. T092e recorded the original conformance-doc
command transcript on 2026-06-30. That run failed closed because
`tests/fuzz/fuzz_binary_types.c` was still a placeholder/input-discard harness.
T102b removed that blocker and reran the focused conformance-doc gate.

### Required Conformance-Doc Commands

| Check | Coverage | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Conformance-doc unit gates | FR-018, FR-019, FR-020, SC-005, SC-006; T075a-T078b | `ctest --test-dir build/test -R '^test_(conformance_docs|traceability_docs)$' --output-on-failure` | CTest exit code; total/passed/failed/skipped count; `test_conformance_docs` claim-filter result; `test_traceability_docs` placeholder and mapping result; failing test names; `build/test/Testing/Temporary/LastTest.log` path. | PASS | T102b evidence: after replacing `fuzz_binary_types`, `cmake --build build/test --target test_conformance_docs` and `ctest --test-dir build/test -R '^test_conformance_docs$' --output-on-failure` passed. The T092e failure on placeholder marker and input discard is historical pre-remediation evidence. |
| Unsupported compliance-claim grep | FR-018, SC-005; OPC-10000-7 sections 4.2 and 4.3; T101a/T101b | `grep -rniE 'profile-compliant|CTT-verified' docs specs | grep -viE 'no (conformance or )?(profile-compliant|CTT-verified)|not (profile-compliant|CTT-verified|claim)|without (actual )?(CTT|external|matching)|until|unless|before any|do not claim|must not claim|profile-targeting|evidence'` | Unfiltered match count; filtered unsupported affirmative-claim count; allowed negative/policy matches; file/line list for any required doc fix; transcript path. | PASS for current conformance docs; repo-wide historical matches reviewed | T101a command exited `0` because repo-wide filtered matches remain: 45 unfiltered matches and 8 filtered matches, with 0 stderr lines. The 8 filtered lines are in `specs/019-fix-conformance-size/*` and `specs/020-audit-hardening/*`; they are historical, planning, or test wording, not current conformance claim docs needing a fix. No unsupported affirmative `profile-compliant` or `CTT-verified` claim was found in `docs/conformance` or `docs/traceability/conformance-claims.md`; current conformance claim docs have 0 unsupported affirmative claims. Transcript path: `/tmp/muc-opcua-t101a/profile-compliance-claim-grep.{command,stdout,stderr,exit-code}.txt`. |
| Quickstart documentation sweep | FR-018, FR-019, FR-020, SC-005, SC-006; T101a-T101f | `rg -n "profile-compliant|CTT-verified|Traceability mapped|Placeholder replaced|TBD" docs specs/020-audit-hardening` | Match count split by unsupported claim, traceability placeholder, and intentional validation placeholder; unexpected matches requiring follow-up; reviewed artifact list. | PARTIAL / FOLLOW-UP | T101c placeholder traceability subset command `rg -n "Traceability mapped|Placeholder replaced|\| TBD \||TBD" docs/traceability docs/conformance specs/020-audit-hardening` exited `0` with 110 matches and 0 stderr lines; transcripts: `/tmp/muc-opcua-t101c/traceability-placeholder-grep.{command,stdout,stderr,exit-code}.txt`. Affected files were `docs/traceability/files-to-sections.md` (65 matched lines with compatibility `Traceability mapped` / `Placeholder replaced` rows), `docs/traceability/020-audit-hardening.md` (44 matched lines with active Feature 020 `TBD` traceability cells), and `specs/020-audit-hardening/quickstart.md` (1 matched line documenting the sweep command itself). Pattern counts: `Traceability mapped` 66, `Placeholder replaced` 66, `| TBD |` rows 42, lines containing `TBD` 45. This remains follow-up, not closure: matches are consistent with documented active Feature 020 placeholders and compatibility rows, including the known `fuzz_binary_types` traceability reference, but they still require cleanup/review before closing the documentation sweep. |
| StatusCode and OPC citation sweep | FR-018, FR-019, FR-020, SC-005, SC-006; OPC-10000-4 section 7.38.2; T101e/T101f | `rg -n "Bad_[A-Za-z0-9_]+|OPC-10000-" specs/020-audit-hardening docs/traceability docs/conformance` | StatusCode names and numeric-value docs reviewed; exact OPC UA part/section citations present for protocol claims; loose or stale citations listed; missing citation follow-ups. | PASS for StatusCode docs; broad citation sweep reviewed; traceability follow-up remains | T101e command exited `0` with 590 matching output lines and 0 stderr lines; transcripts: `/tmp/muc-opcua-t101e/statuscode-opc-citation-grep.{command,stdout,stderr,exit-code}.txt`. Occurrence counts in stdout were 246 `Bad_*` StatusCode matches and 726 `OPC-10000-` citation matches. Top matched files were `specs/020-audit-hardening/tasks.md` (229 lines), `docs/traceability/files-to-sections.md` (68), `docs/conformance/status.md` (40), and `docs/traceability/statuscodes.md` (32); broad scope intentionally includes historical traceability files. The focused `test_conformance_docs` StatusCode name and numeric-value checks from T092e passed, and `docs/conformance/status.md` lists the audit-hardening StatusCodes with exact numeric values and OPC-10000-4 section 7.38.2 citations. Broad citation output remains review evidence rather than a zero-match gate. The 21 active `docs/traceability/020-audit-hardening.md` rows surfaced by this grep have `TBD` outcome cells with OPC citations, so they remain traceability completion follow-up rather than missing-citation evidence. |
| Conformance evidence sync | FR-018, FR-019, FR-020, SC-005, SC-006; T079a-T084d, T092e | `rg -n "FR-018|FR-019|FR-020|SC-005|SC-006|T075|T076|T077|T079|T080|T081|T082|T083|T084|T101" specs/020-audit-hardening docs/validation/audit-hardening.md docs/traceability/020-audit-hardening.md` | Cross-reference that the active feature, this ledger, and traceability skeleton all name the conformance-doc gates and later evidence tasks; missing cross-reference list. | RECORDED / PARTIAL | T092e command exited `0` with 101 matches across the active spec/task/traceability/validation files. FR-018 through FR-020, SC-005/SC-006, T075-T084, and T101 are cross-referenced, but `docs/traceability/020-audit-hardening.md` still has `TBD` outcome cells and later T101 evidence tasks remain open. |

### Focused Conformance-Doc Evidence Rows

Run these focused commands after the referenced documentation and doc-test tasks
exist. If a later task changes file names, claim labels, or traceability table
locations, update the relevant row before recording evidence.

| Evidence row | Coverage | Command | Expected result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Profile claim honesty | FR-018, SC-005; OPC-001, OPC-009; OPC-10000-7 sections 4.2 and 4.3 | `rg -n "profile-targeting|profile-compliant|CTT-verified|Compliance Test Tool|OPC-10000-7" docs/conformance/status.md docs/conformance/profile-embedded.md docs/conformance/profile-micro.md docs/conformance/profile-nano.md docs/traceability/conformance-claims.md` | Claim labels found; evidence for any non-profile-targeting status; unsupported `profile-compliant` or `CTT-verified` claim count; CTT artifact path if one is introduced later. | PASS | T092e command exited `0` with 21 matches. Current docs keep Feature 020 status `profile-targeting`; `docs/conformance/status.md` states no OPC Foundation CTT verification is recorded, `docs/conformance/profile-embedded.md` cites OPC-10000-7 sections 4.2/4.3 for the profile basis, and `docs/traceability/conformance-claims.md` keeps CTT/profile-compliance claims withheld. No external CTT artifact path exists. |
| Service and profile citation exactness | FR-018, FR-020, SC-005, SC-006; OPC-002 through OPC-006, OPC-008 | `rg -n "FindServers|GetEndpoints|OpenSecureChannel|CreateSession|ActivateSession|Read|Browse|Write|CreateMonitoredItems|ModifyMonitoredItems|Publish|Query|History|AggregateFilter|OPC-10000-" docs/conformance/services.md docs/conformance/profile-embedded.md docs/conformance/profile-micro.md docs/conformance/profile-nano.md` | Service/profile rows reviewed; exact OPC UA part and section for each public protocol claim; unsupported or disabled features explicitly scoped; missing or broad citation list. | PASS for Feature 020 target docs | T092e command exited `0` with 76 matches. `docs/conformance/services.md` names the current service surface with exact Part 4 service sections, profile-targeting wording under OPC-10000-7 sections 4.2/4.3, unsupported `TransferSubscriptions`, and scoped AggregateFilter citations to OPC-10000-4 section 7.22.4 plus OPC-10000-13 Average/Minimum/Maximum sections. `docs/conformance/profile-embedded.md` cites the Embedded profile target, transport/encoding sections, and scoped feature rows. Lower-tier `profile-micro.md` and `profile-nano.md` remain orientation/profile ladder docs and do not promote Feature 020 to profile-compliant or CTT-verified status. |
| SecurityPolicy None and identity-token claims | FR-008, FR-018, FR-019, SC-003, SC-005; OPC-009 | `rg -n "SecurityPolicy#?None|SecurityPolicy - None|UserName|password|Anonymous|OPC-10000-4|5\\.5\\.4\\.2|5\\.7\\.3|7\\.40\\.2\\.1" docs/conformance/security.md docs/conformance/identity-policy.md docs/conformance/services.md docs/conformance/status.md` | Username/password-over-None claim status; anonymous-over-None scope; exact GetEndpoints, ActivateSession, and user-token citations; required follow-up rows. | PASS | T092e command exited `0` with 59 matches. `docs/conformance/security.md` documents `SecurityPolicy#None` as anonymous/non-production interoperability metadata and cites OPC-10000-4 section 5.5.4.2 for `GetEndpoints`. `docs/conformance/identity-policy.md` cites OPC-10000-4 sections 5.7.3 and 5.7.3.2 for `ActivateSession`, states `AnonymousIdentityToken` over None remains accepted, and states `UserNameIdentityToken` over None is not advertised and is rejected by default with `Bad_IdentityTokenRejected`; the evidence row cites OPC-10000-4 section 5.7.3.3 for the activation result. |
| StatusCode documentation consistency | FR-012, FR-019, FR-020, SC-004, SC-006; OPC-007; T076a/T076b, T081a/T081b | `rg -n "Bad_[A-Za-z0-9_]+|0x[0-9A-Fa-f]{8}|OPC-10000-4 section 7\\.38\\.2|OPC-10000-4 §7\\.38\\.2" docs/conformance/status.md docs/traceability/statuscodes.md include/muc_opcua/status.h src/core/status.c tests/unit/test_status.c` | StatusCode name list; numeric values; source/test/doc mismatches; exact common StatusCodes citation; missing required StatusCodes from FR-012 and OPC-007. | PASS | T092e command exited `0` with 234 matches. The focused `test_conformance_docs` StatusCode name and numeric-value tests passed, and the current status documentation lists all audit-hardening StatusCodes with hex values matching `include/muc_opcua/status.h` and OPC-10000-4 section 7.38.2 citations. |
| Traceability placeholder cleanup | FR-018, FR-019, FR-020, SC-005, SC-006; T077a/T077b, T082a-T083 | `rg -n "Traceability mapped|Placeholder replaced|\\| TBD \\||TBD" docs/traceability docs/conformance specs/020-audit-hardening` | Placeholder count; affected file/line list; approved active-feature planning placeholders still awaiting later evidence; unexpected conformance or traceability placeholders to remove before closure. | PARTIAL / FOLLOW-UP | T101c command exited `0` with 110 matches and 0 stderr lines; transcripts: `/tmp/muc-opcua-t101c/traceability-placeholder-grep.{command,stdout,stderr,exit-code}.txt`. Affected files: `docs/traceability/files-to-sections.md` (65 matched lines with compatibility `Traceability mapped` / `Placeholder replaced` rows, including the known `fuzz_binary_types.c` reference), `docs/traceability/020-audit-hardening.md` (44 matched lines with active Feature 020 `TBD` traceability cells), and `specs/020-audit-hardening/quickstart.md` (1 matched line for the documented sweep command). Pattern counts: `Traceability mapped` 66, `Placeholder replaced` 66, `| TBD |` rows 42, lines containing `TBD` 45. This is partial follow-up evidence only: remaining matches are consistent with documented active Feature 020 placeholders and compatibility traceability rows, but cleanup/review is still required before closure. |

## Formatting/Static-Analysis Validation Checklist

Coverage intent: this formatting/static-analysis gate supports FR-020 and
SC-006 by requiring formatting, cppcheck, generated static-analysis targets,
and advisory clang-tidy evidence before release validation claims for
`020-audit-hardening`.

T005g did not run these commands. Record tool versions, generated target
availability, exit codes, diagnostics, changed-file scope, and transcript paths
when the later run/record tasks execute. If `clang-format`, `cppcheck`, or
`clang-tidy` is unavailable, record the configure/build warning and tool lookup
result as blocked or advisory evidence rather than as a pass.

### Required Formatting/Static-Analysis Commands

| Check | Coverage | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Tool version inventory | FR-020, SC-006 | `clang-format --version && cppcheck --version && clang-tidy --version` | Tool versions; missing tool names; PATH used by the validation environment; whether missing tools make a required gate blocked or an advisory row unavailable. | PASS | T092g command exited `0`. Versions: `clang-format` Ubuntu 18.1.3, `cppcheck` 2.13.0, `clang-tidy` Ubuntu LLVM 18.1.3. Transcript `/tmp/muc-opcua-t092g/tool-version-inventory.log`. |
| Static-analysis configure | FR-020, SC-006 | `cmake -S . -B build/test -DMUC_OPCUA_BUILD_TESTS=ON` | CMake exit code; compiler; `compile_commands.json` status; generated `format-check`, `cppcheck`, and `clang-tidy` target availability; configure transcript path. | PASS | T092g command exited `0`; CMake wrote build files to `/home/quackdcs/muc-opcua/build/test`, and `build/test/compile_commands.json` exists. No missing-tool warning was recorded in the configure transcript. Transcript `/tmp/muc-opcua-t092g/static-analysis-configure.log`. |
| Generated target inventory | FR-020, SC-006 | `cmake --build build/test --target help | rg '(^| )(format-check|cppcheck|clang-tidy)( |$)'` | Target list output; missing generated targets; associated CMake warning for any missing tool; decision whether clang-tidy remains advisory. | PASS | T092g command exited `0` and found all expected generated targets: `clang-tidy`, `cppcheck`, and `format-check`. Transcript `/tmp/muc-opcua-t092g/generated-target-inventory.log`. |
| Repository format target | FR-020, SC-006 | `cmake --build build/test --target format-check` | Target exit code; files requiring formatting; distinction between active-feature formatting failures and pre-existing repository formatting debt; transcript path. | PASS | T102b evidence: after applying `clang-format -i` to C/H files under `src`, `include`, and `tests`, `cmake --build build/test --target format-check` exited `0` and printed `Built target format-check`. Historical T092g evidence before remediation failed with 858 diagnostics and remains superseded by this rerun. |
| Feature-scope clang-format check | FR-020, SC-006 | `files=$({ git diff --name-only --diff-filter=ACM -- src include tests; git ls-files --others --exclude-standard -- src include tests; } | sort -u | rg '\\.(c|h)$'); printf '%s\n' "$files" | xargs --no-run-if-empty clang-format --dry-run --Werror` | Active-feature C/H file list; clang-format exit code; formatted-file diagnostics; confirmation that only changed or untracked feature files are in scope; transcript path. | PASS | T102b evidence: the feature-scoped `clang-format --dry-run --Werror` command exited `0` after the formatting refresh and emitted no diagnostics. Historical T092g evidence before remediation failed with 734 diagnostics and is superseded. |
| cppcheck target | FR-020, SC-006 | `cmake --build build/test --target cppcheck` | `cppcheck` exit code; warning/performance/portability diagnostics; suppressions used by `cmake/MucOpcUaStaticAnalysis.cmake`; transcript path. | PASS | T102b evidence: `cmake --build build/test --target cppcheck` exited `0`, checked 152 files, emitted no warning/performance/portability/error diagnostics, and only reported the informational `toomanyconfigs` note about checking 12 `#ifdef` configurations. |
| Generated clang-tidy target advisory | FR-020, SC-006 | `cmake --build build/test --target clang-tidy` | Target existence; `clang-tidy` exit code if available; diagnostic count; generated/header diagnostics classified separately; advisory/blocking decision and transcript path. | ADVISORY / WARNINGS | T092g command exited `0` and processed 151 files. The generated target emitted 488 `warning:` diagnostic lines plus generated/header suppression noise, then built target `clang-tidy`; warnings are advisory and do not override the failing formatting gate. Transcript `/tmp/muc-opcua-t092g/generated-clang-tidy-target.log`. |
| Feature-scope clang-tidy advisory | FR-020, SC-006 | `files=$({ git diff --name-only --diff-filter=ACM -- src include tests; git ls-files --others --exclude-standard -- src include tests; } | sort -u | rg '\\.c$'); printf '%s\n' "$files" | xargs --no-run-if-empty clang-tidy -p build/test --checks=-clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling --quiet` | Active-feature C file list; `compile_commands.json` path; clang-tidy diagnostics; known generated/header noise; advisory follow-up list. | ADVISORY / WARNINGS | T092g command exited `0`. The changed/untracked feature scope contained 60 C files and used `build/test/compile_commands.json`; `clang-tidy` reported 18 `warning:` diagnostic lines, including dead-store and uninitialized-value findings in test code such as `tests/integration/test_response_regression.c`, `tests/integration/test_subscriptions.c`, and `tests/unit/test_service_state_errors.c`. Transcript `/tmp/muc-opcua-t092g/feature-scope-clang-tidy.log`. |
| Static-analysis evidence sync | FR-020, SC-006 | `rg -n "020-audit-hardening|format-check|clang-format|cppcheck|clang-tidy|build/test" docs/validation/static-analysis.md docs/validation/audit-hardening.md` | Cross-reference that this feature's formatting/static-analysis command/result is recorded in both validation ledgers when later evidence tasks execute; missing or stale rows to update. | PASS / RECORDED | T102b sync records current pass evidence in both `docs/validation/static-analysis.md` and this ledger: repo-wide `format-check` passes, feature-scoped `clang-format --dry-run --Werror` passes, and `cppcheck` passes. Historical T092g/T098c formatting failures remain documented as pre-remediation evidence; `clang-tidy` remains advisory with warning-cleanup deferred separately. |

## Latency-Threshold Validation Checklist

Coverage intent: this latency gate supports the `020-audit-hardening` plan
performance goal, FR-020, and SC-006 by preserving valid host-flow latency while
hardening Discovery, Session, and Read paths. The flow covers the valid minimal
Discovery/session/read sequence relevant to OPC-002, OPC-003, and OPC-004.

T005h did not run latency tests and does not establish a baseline. T005i ran
the pre-change host baseline, and T005j records it below before later
post-change comparison tasks use this section. The required comparison rule is:
`post_change <= pre_change * 1.05`.

The host test build now provides the `audit_latency_benchmark` target for this
gate. The benchmark runs an in-process OpenSecureChannel, FindServers,
GetEndpoints, CreateSession, ActivateSession, and Read sequence with static
request bodies and no network or sleeps. The pre-change artifact is recorded
below; no pass/fail decision is made until
the post-change comparison artifact exists.

### Required Latency Commands

| Check | Coverage | Command | Expected artifact / result fields | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| Latency command discovery | Plan performance goal; FR-020, SC-006 | `cmake --build build/test --target audit_latency_benchmark && test -x build/test/tests/benchmark/audit_latency_benchmark && rg -n -e audit_latency_benchmark -e valid-minimal-discovery-session-read tests/benchmark docs/validation/audit-hardening.md` | `audit_latency_benchmark` target presence; binary path `build/test/tests/benchmark/audit_latency_benchmark`; checked source/doc paths; discovery transcript path. | PASS | T092h command exited `0`; build confirmed target `audit_latency_benchmark`; executable `build/test/tests/benchmark/audit_latency_benchmark` exists; `rg` found the scenario/target in `docs/validation/audit-hardening.md`, `tests/benchmark/audit_latency_benchmark.c`, and `tests/benchmark/CMakeLists.txt`. Transcript `/tmp/muc-opcua-t092h/latency-command-discovery.log`; exit record `/tmp/muc-opcua-t092h/latency-command-discovery.exit`. |
| Pre-change latency baseline command | Plan performance goal; FR-020, SC-006; T005i/T005j | `cmake -S . -B build/test -DMUC_OPCUA_BUILD_TESTS=ON && mkdir -p docs/validation/artifacts && cmake --build build/test --target audit_latency_benchmark && build/test/tests/benchmark/audit_latency_benchmark --scenario valid-minimal-discovery-session-read --iterations 1000 --warmup 100 --output docs/validation/artifacts/020-audit-hardening-latency-pre-change.json > docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stdout 2> docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stderr` | Baseline artifact path; commit SHA; build dir; compiler and flags; host CPU/governor/load notes; JSON fields `scenario`, `iterations`, `warmup`, `latency_ns_total`, `elapsed_ns`, `mean_ns`, `unit`, `command_build_note`; `pre_change = mean_ns`; exit code; transcript path. | Baseline recorded; post-change comparison TBD. | Exit code `0`; `pre_change = 2345 ns`; artifact `docs/validation/artifacts/020-audit-hardening-latency-pre-change.json`; stdout transcript `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stdout`; stderr transcript `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stderr` empty; commit `0e8aa595d3ba3f42bbbd5603efabe3addf4c7d79`; build dir `build/test`; compiler `/usr/bin/cc`; `CMAKE_C_FLAGS=` empty; `CMAKE_BUILD_TYPE=` empty; Linux `6.18.35-quackplc`, AMD EPYC 4245P, 12 CPUs, governor `performance`, load average `1.29, 1.63, 2.15`. |
| Post-change latency comparison command | Plan performance goal; FR-020, SC-006; T098e/T098f | `mkdir -p docs/validation/artifacts && cmake --build build/test --target audit_latency_benchmark && build/test/tests/benchmark/audit_latency_benchmark --scenario valid-minimal-discovery-session-read --iterations 1000 --warmup 100 --output docs/validation/artifacts/020-audit-hardening-latency-post-change.json` | Post-change artifact path; commit SHA; same host/build settings as baseline or documented variance; JSON fields `scenario`, `iterations`, `warmup`, `latency_ns_total`, `elapsed_ns`, `mean_ns`, `unit`, `command_build_note`; `post_change = mean_ns`; allowed maximum `pre_change * 1.05`; formula result for `post_change <= pre_change * 1.05`; exit code; transcript path. | PASS | T102b latency refresh: command exited `0` after replacing the benchmark's stale hardcoded session token with the fake-platform first token. Artifact `docs/validation/artifacts/020-audit-hardening-latency-post-change.json` records `post_change = 1194 ns`, `iterations=1000`, `warmup=100`, `latency_ns_total=1194778`, `elapsed_ns=1194778`, unit `ns`. Allowed maximum from baseline is `2462.25 ns`; `1194 <= 2462.25`, so the 5% latency threshold passes. |

### T005i Pre-Change Baseline Evidence

T005i establishes only the pre-change baseline for the valid minimal
Discovery/session/read host flow. It does not establish any post-change
pass/fail result.

Command run:

```sh
cmake -S . -B build/test -DMUC_OPCUA_BUILD_TESTS=ON &&
mkdir -p docs/validation/artifacts &&
cmake --build build/test --target audit_latency_benchmark &&
build/test/tests/benchmark/audit_latency_benchmark --scenario valid-minimal-discovery-session-read --iterations 1000 --warmup 100 --output docs/validation/artifacts/020-audit-hardening-latency-pre-change.json > docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stdout 2> docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stderr
```

- Result: exit code `0`; stderr transcript empty.
- Artifact: `docs/validation/artifacts/020-audit-hardening-latency-pre-change.json`.
- Transcripts: `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stdout` and `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stderr`.
- Baseline: `pre_change = 2345 ns` from `mean_ns`.
- JSON fields: `scenario=valid-minimal-discovery-session-read`, `iterations=1000`, `warmup=100`, `latency_ns_total=2345588`, `elapsed_ns=2345588`, `mean_ns=2345`, `unit=ns`.
- Benchmark note: host in-process service dispatch covering OpenSecureChannel, FindServers, GetEndpoints, CreateSession, ActivateSession, and Read; no network or sleeps.
- Environment: commit `0e8aa595d3ba3f42bbbd5603efabe3addf4c7d79`; build dir `build/test`; compiler `/usr/bin/cc`; `CMAKE_C_FLAGS=` empty; `CMAKE_BUILD_TYPE=` empty; Linux `6.18.35-quackplc`; AMD EPYC 4245P; 12 CPUs; governor observed as `performance`; load average `1.29, 1.63, 2.15`.

### 5% Comparison Worksheet

Fill these fields only from the latency command artifacts. CTest duration,
build duration, or interop timeout values are not valid substitutes for the
minimal-flow latency measurement.

| Field | Required value or comparison | Status | Evidence |
| --- | --- | --- | --- |
| Pre-change baseline | `pre_change = 2345 ns` from the T005i/T005j host baseline artifact. | Baseline recorded; post-change comparison TBD. | Artifact `docs/validation/artifacts/020-audit-hardening-latency-pre-change.json`; stdout transcript `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stdout`; stderr transcript `docs/validation/artifacts/020-audit-hardening-latency-pre-change.benchmark.stderr` empty. |
| Post-change measurement | `post_change = 1194 ns` from `docs/validation/artifacts/020-audit-hardening-latency-post-change.json`. | PASS | T102b command exited `0` and wrote the post-change artifact. JSON fields: `iterations=1000`, `warmup=100`, `latency_ns_total=1194778`, `elapsed_ns=1194778`, `mean_ns=1194`, `unit=ns`. |
| Allowed post-change latency | `allowed = 2345 ns * 1.05 = 2462.25 ns`. | PASS | Derived from `docs/validation/artifacts/020-audit-hardening-latency-pre-change.json`. |
| Threshold decision | Pass only when `post_change <= pre_change * 1.05`; `1194 ns <= 2462.25 ns`. | PASS | The stale authentication-token benchmark failure is fixed and the post-change measurement is below threshold. |
